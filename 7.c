#include<stdio.h>
int main(){
    int h,score,no,i=0;
    repeat:
        printf("enter the score of players:");
        scanf("%d",&score);
        printf("enter the no.of player: %d",i);
        scanf("%d",&no);
        h=score;
        if(score\)
}